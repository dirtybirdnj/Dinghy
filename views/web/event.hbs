<div class="container">
    <h2>RasphotoD - Event {{event.id}}</h2>
    <div class="row marketing">
        <div class="col-lg-12">
            {{#each event.Images}}
            <img src="http://localhost:3000/images/{{this.localPath}}" style=" display: inline; float: left;"/>

            <!--<a href="images/{{this.id}}" >-->
                <p>{{this.createdAt}}</hp>
                <p>{{this.localPath}}</p>
            <!-- </a> -->
            {{/each}}
        </div>
    </div>
</div> <!-- /container -->